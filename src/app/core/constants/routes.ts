/** Caminhos de rota centralizados — evita strings mágicas em navegações. */
export const APP_ROUTES = {
  ONBOARDING: '/bem-vindo',
  DASHBOARD: '/',
  TRANSACTIONS: '/transacoes',
  CALENDAR: '/calendario',
} as const;
